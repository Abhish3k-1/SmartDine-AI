// ============================================================
// SmartDine AI — Frontend Configuration
// ============================================================

window.APP_CONFIG = {
    // Mode can be 'emulator' (offline demo) or 'cloud' (Supabase/PHP)
    // Use 'cloud' after Supabase tables are created and populated with SUPABASE_SETUP.sql.
    MODE: 'cloud'
};
